os: linux

language: python

python:
  - "3.8"
  - "3.9"

addons:
  apt:
    packages:
      - libgnutls-dev

# command to install dependencies
install:
  - pip3 install -r requirements.txt
# command to run tests
script:
  - pytest